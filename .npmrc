# Keep pnpm's default isolated node_modules layout.
shamefully-hoist=false

# Electron's postinstall + runtime resolution behaves badly under a strictly
# isolated store, so hoist electron packages to the root node_modules.
public-hoist-pattern[]=*electron*
